#include <stdio.h>
/**
 * main : Program using puts function in C to print a message
 * return : 0 program has terminated successfully.
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
