#include "main.h"

/**
 * _strlen -  Entry point
 * Description: A function that returns the length of a string.
 * @s: A string
 * Return: the length of the string s.
 */
int _strlen(char *s)
{
	int length = 0;

	while (s[len] != '\0')
		length++;
	return (length);
}
