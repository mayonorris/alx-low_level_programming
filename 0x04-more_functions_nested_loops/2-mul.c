#include "main.h"
/**
 * mul - A fuction that compute the multiplication of two integers.
 * @a: First integer
 * @b: Second integer
 * Return: a times b.
 */
int mul(int a, int b)
{
	return (a * b);
}
